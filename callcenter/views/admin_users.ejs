<!DOCTYPE html>
<html>
<head>
    <title>Управление пользователями</title>
    <link rel="stylesheet" href="<%= baseUrl %>/fonts.css">
    <style>
        :root { --bg-body: #E5E5E5; --orange-accent: #E86B35; --card-shadow: 0 4px 12px rgba(0,0,0,0.05); }
        body { margin: 0; padding: 0; background-color: var(--bg-body); display: flex; font-family: 'Manrope', sans-serif; }
        .main-content { flex: 1; margin-left: 260px; padding: 40px; }
        .container { background: white; padding: 30px; border-radius: 15px; box-shadow: var(--card-shadow); }
        .form-group { margin-bottom: 20px; }
        label { display: block; margin-bottom: 5px; font-weight: 600; }
        input { width: 100%; padding: 12px; border: 1px solid #ddd; border-radius: 8px; box-sizing: border-box; }
        .btn-add { background: var(--orange-accent); color: white; border: none; padding: 12px 25px; border-radius: 8px; cursor: pointer; font-weight: 700; }
        table { width: 100%; border-collapse: collapse; margin-top: 30px; }
        th, td { text-align: left; padding: 15px; border-bottom: 1px solid #eee; }
        th { color: #999; font-weight: 500; font-size: 13px; }
    </style>
</head>
<body>
    <%- include('sidebar') %>
    <div class="main-content">
        <div class="container">
            <h2>Создать администратора</h2>
            <div style="max-width: 400px;">
                <div class="form-group">
                    <label>ФИО</label>
                    <input type="text" id="fullName" placeholder="Иванов Иван Иванович">
                </div>
                <div class="form-group">
                    <label>Почта (Логин)</label>
                    <input type="email" id="email" placeholder="admin@example.com">
                </div>
                <div class="form-group">
                    <label>Пароль</label>
                    <input type="password" id="password">
                </div>
                <button class="btn-add" onclick="createUser()">Создать аккаунт</button>
            </div>

            <hr style="margin: 40px 0; border: none; border-top: 1px solid #eee;">

            <h2>Список администраторов</h2>
            <table>
                <thead>
                    <tr><th>ФИО</th><th>Почта</th><th>Роль</th></tr>
                </thead>
                <tbody>
                    <% users.forEach(u => { %>
                        <tr>
                            <td><%= u.fullName %></td>
                            <td><%= u.email %></td>
                            <td><%= u.role %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <script>
        async function createUser() {
            const fullName = document.getElementById('fullName').value;
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            if(!fullName || !email || !password) return alert('Заполните все поля');

            const res = await fetch('<%= baseUrl %>/api/admin/users', {
                method: 'POST',
                headers: {'Content-Type': 'application/json'},
                body: JSON.stringify({ fullName, email, password })
            });

            if(res.ok) {
                alert('Пользователь создан');
                window.location.reload();
            } else {
                const data = await res.json();
                alert('Ошибка при создании: ' + (data.error || 'неизвестно'));
            }
        }
    </script>
</body>
</html>