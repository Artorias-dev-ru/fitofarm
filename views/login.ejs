<!DOCTYPE html>
<html>
<head>
    <title>Вход</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="center">
    <div class="login-box">
        <img src="/logo.svg" alt="ФИТОФАРМ">
        <% if(error) { %> <p><%= error %></p> <% } %>
        <form action="/login" method="POST" class="flex-col" style="gap:1vh;">
            <input type="password" name="password" placeholder="Введите пароль (admin)">
            <button type="submit">Войти</button>
        </form>
    </div>
</body>
</html>