<!DOCTYPE html>
<html>
<head>
    <title>Вход</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="<%= baseUrl %>/style.css">
</head>
<body style="align-items: center; justify-content: center;">
    <div class="login-box">
        <div style="text-align: center;">
            <% if (baseUrl !== '/callcenter') { %>
                <img src="<%= baseUrl %>/logo.svg" alt="Logo">
            <% } else { %>
                <div style="font-size: 24px; font-weight: 700; color: #333; font-family: 'Inter', sans-serif; margin-bottom: 20px;">
                    Колл-центр
                </div>
            <% } %>
        </div>
        <form action="<%= baseUrl %>/login" method="POST">
            <input type="text" name="login" placeholder="Email или Логин" required>
            <input type="password" name="password" placeholder="Пароль" required>
            <button type="submit">Войти</button>
        </form>
        <% if (error) { %>
            <p style="text-align: center; margin-top: 10px;"><%= error %></p>
        <% } %>
    </div>
</body>
</html>