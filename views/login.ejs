<!DOCTYPE html>
<html>
<head>
    <title>Вход</title>
    <link rel="stylesheet" href="<%= baseUrl %>/style.css">
</head>
<body class="center">
    <div class="login-box">
        <img src="<%= baseUrl %>/logo.svg" alt="ФИТОФАРМ">
        <% if(error) { %> <p><%= error %></p> <% } %>
        <form action="<%= baseUrl %>/login" method="POST" class="flex-col" style="gap:1vh;">
            <input type="text" name="username" placeholder="Логин" required>
            <input type="password" name="password" placeholder="Пароль" required>
            <button type="submit">Войти</button>
        </form>
    </div>
</body>
</html>